config:
  send_anonymous_usage_stats: false

default:
  target: dev
  outputs:
    dev:
      type: postgres
      host: db
      user: postgres
      password: postgres
      port: 5432
      dbname: postgres
      schema: dbt
      threads: 4
